apiVersion: v1
kind: Service
metadata:
  name: {{ template "bspimplementation.fullname" . }}
  labels:
    app: {{ template "bspimplementation.name" . }}
    chart: {{ template "bspimplementation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "bspimplementation.name" . }}
    release: {{ .Release.Name }}
