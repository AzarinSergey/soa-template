apiVersion: v1
kind: Service
metadata:
  name: {{ include "bsp.fullname" . }}
  labels:
    {{- include "bsp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bsp.selectorLabels" . | nindent 4 }}