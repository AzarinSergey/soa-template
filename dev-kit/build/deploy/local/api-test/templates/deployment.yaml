apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "api-test.fullname" . }}
  labels:
    {{- include "api-test.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "api-test.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "api-test.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.deployment.containers.containerPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: {{ .Values.deployment.containers.livenessProbe.getPath }}
              port: {{ .Values.deployment.containers.containerPort }}
            periodSeconds: 30
            initialDelaySeconds: 30
          env:
          - name: ASPNETCORE_ENVIRONMENT
            value: {{ .Values.infrastructure.environmentname }}
          - name: ApplicationConfig__AppName
            value: {{ .Values.infrastructure.appname }}
          - name: ServiceConfig__ServiceName
            value: {{ include "api-test.fullname" . }}
          - name: ServiceConfig__RedisConnection
            value: {{ .Values.infrastructure.connections.RedisConnection | quote }}
          - name: ServiceConfig__PostgresConnection
            value: {{ .Values.infrastructure.connections.PostgresConnection | quote }}
