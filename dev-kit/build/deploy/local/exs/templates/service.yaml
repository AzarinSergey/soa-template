apiVersion: v1
kind: Service
metadata:
  name: {{ include "exs.fullname" . }}
  labels:
    {{- include "exs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exs.selectorLabels" . | nindent 4 }}
