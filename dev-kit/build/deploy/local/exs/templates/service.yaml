apiVersion: v1
kind: Service
metadata:
  name: {{ template "exsimplementation.fullname" . }}
  labels:
    app: {{ template "exsimplementation.name" . }}
    chart: {{ template "exsimplementation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "exsimplementation.name" . }}
    release: {{ .Release.Name }}
