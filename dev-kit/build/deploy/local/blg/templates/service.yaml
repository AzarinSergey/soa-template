apiVersion: v1
kind: Service
metadata:
  name: {{ template "blgimplementation.fullname" . }}
  labels:
    app: {{ template "blgimplementation.name" . }}
    chart: {{ template "blgimplementation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blgimplementation.name" . }}
    release: {{ .Release.Name }}
